# ===============================================
# REQUIREMENTS PARA ENTORNO DE DESARROLLO (Python 3.13+)
# ===============================================

# Core framework
fastapi==0.115.0          # compatible con Pydantic 2.12 y Starlette 0.38+
sqlalchemy==2.0.36        # última versión estable compatible con FastAPI

# Seguridad y autenticación
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0             # soporte completo Python 3.13

# Modelado y validación
pydantic[email]==2.12.0   # soporta Python 3.13
email-validator==2.2.0

# Utilidades varias
python-dotenv==1.0.1
python-multipart==0.0.9
debugpy==1.8.6

# ===============================================
# Herramientas necesarias para compilar extensiones Rust
# (se instalan ANTES que pydantic-core si no hay wheel)
# ===============================================
setuptools-rust>=1.10.0
maturin>=1.7.0
